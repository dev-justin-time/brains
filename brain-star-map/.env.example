# Blocks Network credentials — created by `blocks login --write-env`
# Copy this file to .env (and into each blocks/agents/<name>/.env you deploy).
BLOCKS_API_KEY=bk_your_api_key_here

# Optional overrides (defaults shown)
# BLOCKS_BASE_URL=https://app.blocks.ai
# BLOCKS_PROFILE=blocks-network
